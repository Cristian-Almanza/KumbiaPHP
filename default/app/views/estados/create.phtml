
<?php View::content() ?>
<h3>Alta Estados<h3>

<?= Form::open(); // por defecto llama a la misma url ?> 

        <label>Nombre <?= Form::text('estados.nombre') ?></label>
        
      <?= Form::submit('Agregar') ?> 
      <button><?= Html::linkAction("index/", 'Cancelar') ?></button>

<?= Form::close() ?> 